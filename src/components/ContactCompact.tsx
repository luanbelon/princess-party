import ContactFormCompact from './ContactFormCompact';
import styles from './ContactCompact.module.css';

export default function ContactCompact() {
  return (
    <section id="contacto" className={styles.wrapper}>
      <ContactFormCompact />
    </section>
  );
}

